# Archived Capability Salvage

This folder captures the functionality worth preserving from archived repos
after comparing them against the live `paperclip-product-autopilot` repo.

Archived repo reviewed:

- `paperclip-autoresearch-improver`

Outcome:

- keep `paperclip-product-autopilot` as the canonical repo
- salvage evaluator-driven engineering optimization as a mode inside the live
  system
- do not restore the archived repo as a separate product

Artifacts:

- `evaluator-driven-code-optimization-prd.md`
- `archived-autoresearch-improver-porting-matrix.md`

